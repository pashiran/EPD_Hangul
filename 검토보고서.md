# Hangul Display for Arduino — 프로젝트 검토 보고서

- 검토일: 2026-07-21
- 검토 범위: 라이브러리 코어(`HangulDisp/`), 폰트 헤더 135종, 예제(`WowkiExample/`), 변환 도구(`tools/`), 문서(README, 명세서, 사용방법), 빌드 설정(platformio.ini, wokwi.toml)

## 요약

핵심 렌더링 로직(초중종 분해, 벌 선택, 비트맵 합성)은 잘 설계되어 있고 명세 문서도 충실합니다. 그러나 **현재 저장소 상태로는 공식 라이브러리 헤더(`HangulDisp/HangulDisp.h`)와 폰트 헤더 135종이 서로 호환되지 않아 컴파일이 불가능**합니다. 예제가 동작하는 것은 예제 폴더에 남아 있는 구버전 헤더 복사본 덕분입니다. 이 구조체 불일치 해소가 최우선 과제이며, 그 외에 UTF-8 처리 버그 2건, 변환 스크립트 경로 오류, 라이선스 부재 등이 확인되었습니다.

---

## 1. 치명적 문제 (즉시 수정 필요)

### 1-1. `HangulFontInfo` 구조체와 폰트 헤더의 필드 불일치 — 컴파일 불가

- 라이브러리 구조체([HangulDisp.h:95-102](HangulDisp/HangulDisp.h#L95-L102))는 6개 필드입니다: `name, width, height, choData, jungData, jongData` — `hasAscii` 필드가 **없습니다**.
- 그런데 **폰트 헤더 135종 전부**가 `hasAscii` 를 포함한 **7개 값**으로 초기화합니다 (예: [H01_kr.h](HangulDisp/fonts/H01_kr.h) 의 `false, // hasAscii` 줄).
- 변환기([han_to_h.py:189-191](tools/easyview-font-converter/han_to_h.py#L189-L191))도 계속 `hasAscii` 를 생성하도록 되어 있습니다.
- 결과: `HangulDisp/HangulDisp.h` + 폰트 헤더 조합은 "too many initializers" 컴파일 오류가 납니다. 현재 폰트들은 구버전인 `WowkiExample/hangulDisp.h`(=`hasAscii` 필드가 있는 버전)로만 컴파일됩니다.

**조치**: 구조체에 `hasAscii` 를 다시 넣거나(ASCII 지원 계획이 있으므로 권장), 변환기와 폰트 135종에서 필드를 제거해 세 곳(라이브러리·변환기·폰트)을 일치시키세요. `FONT_HEADER_SPECIFICATION.md` 121행의 구조체 정의도 함께 갱신해야 합니다.

### 1-2. 라이브러리 헤더가 두 벌 존재하며 내용이 다름 (drift 발생 중)

- [HangulDisp/HangulDisp.h](HangulDisp/HangulDisp.h) 와 [WowkiExample/hangulDisp.h](WowkiExample/hangulDisp.h) 가 서로 다릅니다:
  - `hasAscii` 필드 유무 (1-1 참조)
  - ASCII 문자 커서 전진량: 예제본 8px vs 라이브러리본 16px
- 예제의 `#include "../HangulDisp/fonts/A_Font_kr.h"` → 폰트 헤더 내부의 `#include "hangulDisp.h"` 가 인클루드 경로 상 **예제 폴더의 구버전 복사본**으로 해석되어, 우연히 빌드가 성공하고 있는 상태입니다.

**조치**: 라이브러리 헤더를 한 곳(`HangulDisp/`)으로 단일화하고 예제에서는 경로로 참조하세요. PlatformIO라면 `platformio.ini` 에 `build_flags = -I HangulDisp -I HangulDisp/fonts` 를 추가하면 복사본 없이 빌드됩니다.

### 1-3. 파일명 대소문자 불일치 — Linux/CI에서 빌드 실패

- 실제 파일명은 `HangulDisp.h`(대문자 H, D)인데, 폰트 헤더 135종과 README, 사용방법.md 는 전부 `#include "hangulDisp.h"`(소문자 h)로 참조합니다.
- Windows에서는 동작하지만 **대소문자를 구분하는 파일시스템(Linux, GitHub Actions CI, 일부 macOS)에서는 인클루드 실패**합니다.

**조치**: 파일명 또는 인클루드 문 한쪽으로 통일하세요. (클래스명이 `hangulDisp` 이므로 파일명을 `hangulDisp.h` 로 맞추는 편이 수정 범위가 작습니다 — 폰트 헤더는 그대로 두면 됩니다.)

---

## 2. 주요 버그

### 2-1. 한글 아닌 3바이트 UTF-8 문자가 "가"로 렌더링됨

- [HangulDisp.h:307-324](HangulDisp/HangulDisp.h#L307-L324) 의 `print()` 는 3바이트 문자를 만나면 무조건 `printHangulChar()` 를 호출합니다.
- 한글이 아니면 `decompose()` 가 all-zero 구조체를 반환하는데([HangulDisp.h:186-191](HangulDisp/HangulDisp.h#L186-L191)), 호출부는 이를 구분하지 않고 `cho+1=1(ㄱ), jung+1=1(ㅏ)` 글리프를 그립니다.
- 결과: `…`, `“ ”`, 한자, 일본어 등 모든 비한글 3바이트 문자가 **"가"처럼 출력**됩니다.

**조치**: `printHangulChar()` 진입 시 `HangulCore::isHangul()` 로 검사해 비한글이면 커서만 전진시키세요.

### 2-2. 4바이트 UTF-8(이모지 등) 처리 오류 — 문자열 파싱 어긋남

- 3바이트 판별 조건 `(*p & 0xE0) == 0xE0` ([HangulDisp.h:308](HangulDisp/HangulDisp.h#L308))은 4바이트 선두 바이트(0xF0~0xF4)에도 참이 됩니다. 4바이트 문자를 3바이트만 소비하고 넘어가므로 이후 문자열 전체가 어긋나 깨진 출력이 나옵니다.
- `isHangul()` ([HangulDisp.h:177-183](HangulDisp/HangulDisp.h#L177-L183))의 같은 마스크 검사도 동일한 문제가 있고, 연속 바이트(`(b & 0xC0) == 0x80`) 검증도 없습니다.

**조치**: 3바이트 판별을 `(*p & 0xF0) == 0xE0` 으로, 4바이트 분기 `(*p & 0xF8) == 0xF0` → `p += 4` 를 추가하세요.

### 2-3. `convert_all.py` 경로 오류 — 현재 구조에서 실행 불가

- [convert_all.py:14-15](tools/easyview-font-converter/convert_all.py#L14-L15) 가 `script_dir/../EasyView-font/ko` 를 참조하는데, 이는 `tools/EasyView-font/ko` 가 되어 존재하지 않습니다. 실제 위치는 저장소 루트의 `EasyView-font/ko` 이므로 `'..'` 가 한 단계 부족합니다. 출력 경로(`tools/HangulDisp/fonts`)도 동일한 문제입니다.

**조치**: `os.path.join(script_dir, '..', '..', 'EasyView-font', 'ko')` 로 수정. (과거 폴더 이동 커밋 이후 스크립트가 따라가지 못한 것으로 보입니다.)

### 2-4. ASCII 문자가 렌더링되지 않음 (문서에 명시 안 됨)

- 라이브러리는 ASCII를 그리지 않고 커서만 16px 전진합니다([HangulDisp.h:315-317](HangulDisp/HangulDisp.h#L315-L317)). 예제 문구 "안녕하세요 한글입니다."의 마침표도 실제로는 출력되지 않습니다.
- README/사용방법.md 어디에도 "ASCII는 미지원"이라는 안내가 없어 사용자가 버그로 오인하기 쉽습니다.

**조치**: 단기적으로 README에 제한사항 명시, 장기적으로 `hasAscii` 폰트의 8×16 ASCII 렌더링 구현(ttf-font-converter README의 수정 계획과 연결됨).

---

## 3. 구조 / 배포 관련 문제

### 3-1. LICENSE 파일 부재 (법적 문제 소지)

- 저장소에 라이선스 파일이 없어 제3자가 합법적으로 사용할 수 없습니다.
- 더 중요한 것은 **EasyView 원본 폰트(.han/.spc, 2.5MB)와 변환된 폰트 헤더 135종의 저작권/재배포 허가 여부**가 어디에도 명시되어 있지 않다는 점입니다. 비트맵 폰트도 국가에 따라 저작권 보호 대상일 수 있습니다.
- `WowkiExample/test.h` 는 NanumGar(나눔) TTF에서 생성된 데이터인데 OFL 고지가 없습니다.

**조치**: 코드에 라이선스(MIT 등) 부여, 폰트 출처·허가 상태를 README에 명시, 불확실하면 원저작자 확인.

### 3-2. Arduino/PlatformIO 라이브러리 규격 미준수

- `library.properties`(Arduino) / `library.json`(PlatformIO) 이 없어 라이브러리 매니저로 설치할 수 없고, `HangulDisp/` 폴더 구조도 표준 레이아웃(`src/`, `examples/`)이 아닙니다. 현재는 사용자가 파일을 직접 복사해야 합니다.

**조치**: 표준 라이브러리 구조로 재편하면 `#include` 경로 문제(1-2, 1-3)도 함께 해결됩니다.

### 3-3. 불필요한 대용량 파일

- [WowkiExample/test.h](WowkiExample/test.h): 10만 줄(약 4.5MB)의 LVGL 포맷 폰트인데 **어디에서도 include되지 않는 사장 코드**입니다.
- `documents/` 7.6MB, `.git` 24MB — 참고자료 PDF/xlsx가 저장소를 무겁게 합니다. 필요하다면 Release 첨부나 별도 저장소 분리를 고려하세요.
- `HangulCore::findNextHangul()` ([HangulDisp.h:214-233](HangulDisp/HangulDisp.h#L214-L233))도 사용처가 없는 죽은 코드입니다.

### 3-4. 테스트·CI 부재

- 렌더링 로직(분해/벌 선택)은 순수 함수라 호스트에서 단위 테스트하기 좋은 구조인데 테스트가 전혀 없습니다. GitHub Actions에서 `pio run` 빌드 체크만 넣어도 1-1~1-3 같은 문제를 자동으로 잡을 수 있습니다.

---

## 4. 문서 문제

| 위치 | 문제 |
|------|------|
| [README.md:152-181](README.md#L152-L181) | `tools/ttf-converter/ttf_to_h.py` 를 안내하지만 실제 폴더명은 `tools/ttf-font-converter` 이고 **`ttf_to_h.py` 스크립트 자체가 존재하지 않음** (GUI 도구 Einstein Bacon Machine 방식으로 바뀌었는데 README 미반영) |
| [README.md:188](README.md#L188) | `[옛한글 텍스트 뷰어 EasyView](EasyView-3.0.b2)` — 존재하지 않는 경로로의 깨진 링크 |
| [README.md:22-44](README.md#L22-L44) | 빠른 시작 예제에 `#include <TFT_eSPI.h>` 누락, `loop()` 에서 매 프레임 전체 렌더링(예제로 부적절) |
| [FONT_HEADER_SPECIFICATION.md:121](FONT_HEADER_SPECIFICATION.md#L121) | 구조체 정의가 실제 폰트 헤더(`hasAscii` 포함)와 불일치 (1-1과 동일 원인) |
| [사용방법.md:9](사용방법.md#L9) | "기본 H01 폰트는 `Font_name.h` 사용" — `Font_name.h` 는 존재하지 않는 플레이스홀더라 혼란 유발 |
| 폴더명 `WowkiExample` | Wokwi 의 오타 (Wowki → Wokwi) |
| CLAUDE.md / AGENTS.md / copilot-instructions.md | 세 파일 모두 프로젝트와 무관한 외부 링크 하나만 담고 있어, AI 도구가 참조할 프로젝트 컨텍스트(구조·규칙·빌드 방법)가 전혀 없음 |

---

## 5. 코드 품질 개선 제안 (선택)

1. **`HG_COLOR_INVERT` 미구현**: [HangulDisp.h:418-426](HangulDisp/HangulDisp.h#L418-L426) 에서 INVERT는 콜백에 리터럴 `2` 를 넘길 뿐이라 사실상 의미가 없습니다. 지원할 수 없으면 enum에서 제거를 권합니다.
2. **개행 미지원**: `print()` 가 `'\n'` 도 폭 16px 공백으로 처리합니다. 개행/자동 줄바꿈 처리가 있으면 실사용성이 크게 올라갑니다.
3. **`HangulFontInfo.width/height` 미사용**: 렌더러가 16×16 상수로 고정되어 있어 필드가 장식입니다. 향후 확장 계획이 없다면 제거, 있다면 렌더러가 이 값을 쓰도록 변경하세요.
4. **`HangulSize` enum 값 비연속(0,2,3,4)**: 의도(비트 플래그?)가 드러나지 않습니다. 주석 또는 값 정리를 권합니다.
5. **생성자 들여쓰기 불규칙**: [HangulDisp.h:260-270](HangulDisp/HangulDisp.h#L260-L270) 포맷팅 정리 필요.
6. `.gitignore` 에 `__pycache__/`, `*.pyc` 추가 권장 (Python 도구 실행 시 생성됨).

---

## 6. 권장 조치 우선순위

| 순위 | 작업 | 관련 항목 |
|------|------|-----------|
| 1 | `HangulFontInfo` 구조체·변환기·폰트 135종·명세서의 `hasAscii` 일치화 | 1-1 |
| 2 | 라이브러리 헤더 단일화 + 파일명 대소문자 통일 | 1-2, 1-3 |
| 3 | UTF-8 파싱 수정 (비한글 3바이트 스킵, 4바이트 처리) | 2-1, 2-2 |
| 4 | `convert_all.py` 경로 수정 | 2-3 |
| 5 | LICENSE 추가 및 폰트 저작권 명시 | 3-1 |
| 6 | README의 TTF 도구 안내·깨진 링크 수정, ASCII 미지원 명시 | 2-4, 4 |
| 7 | 라이브러리 표준 구조화 + CI 빌드 체크 | 3-2, 3-4 |
| 8 | 사장 코드(test.h 등) 정리, 코드 품질 개선 | 3-3, 5 |

---

*이 보고서는 정적 검토 기준이며, 실제 하드웨어/Wokwi 시뮬레이션 동작 검증은 포함하지 않습니다. 1-1 항목은 초기화 값 개수(7개)와 구조체 필드 수(6개)의 불일치로 컴파일 불가가 확정적입니다.*
